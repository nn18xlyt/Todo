{
  "name": "todo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npm --prefix web run dev",
    "build": "npm --prefix web run build && node -e \"const fs=require('fs'); const path=require('path'); fs.rmSync('dist',{recursive:true,force:true}); fs.cpSync(path.join('web','dist'),'dist',{recursive:true});\"",
    "lint": "npm --prefix web run lint",
    "test": "npm --prefix web run test",
    "test:run": "npm --prefix web run test:run",
    "preview": "npm --prefix web run preview",
    "install:web": "npm --prefix web install",
    "postinstall": "npm run install:web"
  }
}
